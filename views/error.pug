doctype html
html
    head 
        title (Error) JMeter Image Upload
        style 
            include ./include/style.css

    body 
        main.main
            .center-container
                h1.heading #{title}
                p #{message}